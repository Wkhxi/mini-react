### 初始化项目

```shell
mkdir mini-react
cd mini-react
npm init -y


npm install --save-dev typescript

# 创建 ts 配置文件
npx tsc --init

# 编译
npx tsc -w


#  index.html 引入下 dist 的代码

# 给 index.html 起一个静态服务
npx http-server .



```
